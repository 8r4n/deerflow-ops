{
  "mcpServers": {
    "github": {
      "enabled": true,
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github@2025.4.8"],
      "env": {
        "GITHUB_TOKEN": "$GITHUB_TOKEN"
      },
      "description": "GitHub MCP server — read/write issues, PRs, code search, progress updates, and mission planning"
    },
    "fetch": {
      "enabled": true,
      "type": "stdio",
      "command": "mcp-server-fetch",
      "args": [],
      "env": {},
      "description": "Web fetch MCP server — ingest external documentation and web sources (Python: mcp-server-fetch==2025.4.7)"
    }
  },
  "skills": {}
}
